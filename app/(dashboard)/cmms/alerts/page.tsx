'use client'

import EnhancedDashboard from '@/app/(dashboard)/cmms/enhanced/page'

export default function AlertsPage() {
  // Quick reuse: enhanced dashboard already has alert-like widgets + predictions.
  return <EnhancedDashboard />
}
